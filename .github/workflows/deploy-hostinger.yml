name: Deploy To Hostinger

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: hostinger-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      APP_DIR: /var/www/expense-lite-ai
      APP_NAME: expense-lite-ai

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.HOSTINGER_SSH_KEY }}

      - name: Resolve SSH Settings
        id: ssh
        env:
          HOST: ${{ secrets.HOSTINGER_HOST }}
          USERNAME: ${{ secrets.HOSTINGER_SSH_USER }}
        run: |
          echo "host=$HOST" >> $GITHUB_OUTPUT
          echo "username=$USERNAME" >> $GITHUB_OUTPUT
          echo "port=22" >> $GITHUB_OUTPUT

      - name: Debug Connection
        env:
          HOST: ${{ secrets.HOSTINGER_HOST }}
        run: |
          echo "Host Length: ${#HOST}"
          echo "User: ${{ steps.ssh.outputs.username }}"
          echo "Port: ${{ steps.ssh.outputs.port }}"

      - name: DNS Lookup
        run: |
          nslookup "${{ steps.ssh.outputs.host }}" || true

      - name: Check Port 22
        run: |
          nc -vz "${{ steps.ssh.outputs.host }}" 22

      - name: Add Host Key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H -p 22 "${{ steps.ssh.outputs.host }}" >> ~/.ssh/known_hosts

      - name: Test SSH Login
        run: |
          ssh \
            -o StrictHostKeyChecking=no \
            -o BatchMode=yes \
            -p 22 \
            "${{ steps.ssh.outputs.username }}@${{ steps.ssh.outputs.host }}" \
            "echo SSH LOGIN SUCCESS"

      - name: Check Remote Environment
        run: |
          ssh \
            -o StrictHostKeyChecking=no \
            -p 22 \
            "${{ steps.ssh.outputs.username }}@${{ steps.ssh.outputs.host }}" \
            "
            hostname
            whoami
            pwd
            node -v || true
            npm -v || true
            pm2 -v || true
            "

      - name: Create Release Archive
        run: |
          tar \
            --warning=no-file-changed \
            --exclude=.git \
            --exclude=.github \
            --exclude=node_modules \
            --exclude=.env \
            --exclude=release.tgz \
            --exclude=data \
            --exclude=backups \
            --exclude=*.log \
            --exclude=.next \
            --exclude=coverage \
            -czf release.tgz . || true

      - name: Upload Release
        run: |
          ssh \
            -o StrictHostKeyChecking=no \
            -p 22 \
            "${{ steps.ssh.outputs.username }}@${{ steps.ssh.outputs.host }}" \
            "mkdir -p $APP_DIR"

          scp \
            -P 22 \
            release.tgz \
            "${{ steps.ssh.outputs.username }}@${{ steps.ssh.outputs.host }}:$APP_DIR/release.tgz"

      - name: Deploy
        run: |
          ssh \
            -o StrictHostKeyChecking=no \
            -p 22 \
            "${{ steps.ssh.outputs.username }}@${{ steps.ssh.outputs.host }}" \
            "
            set -e

            mkdir -p $APP_DIR
            cd $APP_DIR

            tar -xzf release.tgz
            rm -f release.tgz

            npm install --production

            pm2 restart $APP_NAME || pm2 start npm --name $APP_NAME -- start

            pm2 save

            echo 'Deployment completed successfully'
            "